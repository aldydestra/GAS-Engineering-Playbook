# GAS Core Recipes

Reusable recipes should prefer bulk operations and explicit contracts.

Examples:

- archive by filtering arrays then bulk-writing,
- duplicate detection using `Set`/`Map`,
- batch notifications with quota checks,
- multi-sheet processing from explicit configuration,
- deterministic dashboard rebuild.
